import 'package:flutter/material.dart';

class Objectifs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suivre Objectifs '),
      ),
      body: Center(
        child: Text('List Objectifs'),
      ),
    );
  }
}
