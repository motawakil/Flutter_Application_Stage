import 'package:flutter/material.dart';

class Missions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suivre Missions '),
      ),
      body: Center(
        child: Text('List Missions '),
      ),
    );
  }
}
