import 'package:flutter/material.dart';

class Realisations extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Suivre Realisations '),
      ),
      body: Center(
        child: Text('List Realisations '),
      ),
    );
  }
}
